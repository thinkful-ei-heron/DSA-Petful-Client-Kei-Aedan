import React, { Component } from 'react';

export default class Cat extends Component {
  componentDidMount() {
    const cat = this.props.cat;
  }
  render(){
    return (
      <>
      </>
    );
  }
}