import React, { Component } from 'react';

export default class Cat extends Component {
  componentDidMount() {
    const dog = this.props.dog;
  }
  render(){
    return (
      <>
      </>
    );
  }
}